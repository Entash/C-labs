#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, y, z, result,
    printf("Please enter 3 integers: \n");
    scanf("%d" "%d" "%d", &x &y &z); // taking in the 3 ints
    result = (x*y - z); // the math int1 * int2 – int3
    printf("%d", result); // printing the total.
    return 0;
}
